- Arreglar que no se expandan las variables de entorno cuando son la señal de stop de un heredoc
- Arreglar que echo imprima un salto de línea cuando envía a un heredoc (?)
- Arreglar que Ctrl+D devuelva el exit_status, CREO, que bash devuelve 1? No sé, hay que revisarlo.
	- Lo que pasa es que por algún motivo, cuando exit_status es 258, el exit_status deberia ser
	2
	al salir. Pero sale 1.
	Vale, ahora vuelve a dar 1 al salir. No lo entiendo.
- Liada cuando haces unset de PATH, parece que algunos comandos no dan error de ningun tipo
	- Al parecer, el primer error sí lo da. Tipo
		unset PATH
		ls
		minishell: ls: No such file or directory
	- Pero a partir de ahí deja de dar errores
		ls
		ls
		ls
	- Pero si hago uso de un builtin, el siguiente error lo vuelve a procesar
		ls
		ls
		echo hola
		hola
		ls
		minishell: ls: No such file or directory
		ls
		ls
	- ???

- Cuando usamos heredoc pasa lo siguiente:
	cat << EOF hola
	(aqui escribimos hasta que pongamos EOF)
	Y despues de esto, ya está
	-Pero en bash, ese hola del final también se pasa como argumento a cat ?? y dice:
	cat: hola: No such file or directory